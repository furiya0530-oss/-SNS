// ────────────────────────────────────────────────
// アプリ全体で使う「決まりごとの数字」をまとめたファイル
//
// 仕様変更（例：最低文字数を 300 字にしたい）があったときは、
// このファイルの数字を書き換えるだけで全画面に反映されます。
// ────────────────────────────────────────────────

/** 投稿に必要な最低文字数（長文限定 SNS なので 400 字） */
export const MIN_BODY_LENGTH = 400

/** ハッシュタグの上限個数 */
export const MAX_TAGS = 5

/** 今週の運営お題（今は固定の文字列。将来はサーバーから取得する想定） */
export const WEEKLY_THEME = '忘れられない一日'
